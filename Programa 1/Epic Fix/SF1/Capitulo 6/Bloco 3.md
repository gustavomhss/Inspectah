# Bloco 3 — Exemplos e padrões
- Exemplo de alerta firing/resolution: aumentar `flow_rollout_rollback_total` e verificar firing em Alertmanager (snapshot).
- Exemplo de hash drift: publish hash vs runtime diferente → erro + alerta `catalog_hash_drift`.
- Exemplo de auditoria: log com flow_id, flow_version_id, mode, operation_id, actor, catalog_hash.
