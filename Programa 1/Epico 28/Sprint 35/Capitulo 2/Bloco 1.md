# Bloco 1 — Gates G0 e G1
- **G0 — Escopo/catalogo blindado:** 9×4 completo, s35_slos.md como fonte única; catálogo `config/flow_catalog/*.yaml` (news_v2, contestacao_v0) assinado + hash calculado; `bin/s35_bundle.sh` gera manifest e comparação publish/runtime; `bin/s35_g0_scope.sh` falha se hash/assinatura faltar ou divergir.
- **G1 — Modelo/rollout com limites aplicados:** migração `0036_s35_flow_governance_advanced.py` aplicada; modos teste/canary/ativo operacionais; limites (`max_canary_duration_minutes`, `operation_timeout_seconds`, `max_rollbacks_per_hour`, `max_test_percentual`) aplicados no service e cobertos por testes negativos; catálogo carregado do publish e bloqueia drift; `bin/s35_g1_model.sh` roda casos felizes + negativos (tempo, percentual, actor ausente).
